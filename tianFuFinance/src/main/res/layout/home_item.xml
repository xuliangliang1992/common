<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <LinearLayout
            android:id="@+id/ll_show"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <ImageView
                android:id="@+id/img"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="5dp"
                android:layout_marginTop="5dp"
                android:src="@mipmap/ic_launcher" />

            <TextView
                android:id="@+id/tv_title"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_gravity="center_vertical"
                android:layout_marginLeft="5dp"
                android:layout_weight="1"
                android:text="@{`item `+model.num}"
                android:textSize="16dp" />
        </LinearLayout>

        <!-- 屏幕右侧外边部分，正常时在屏幕中处于不可见 -->
        <LinearLayout
            android:id="@+id/ll_hidden"
            android:layout_width="50dp"
            android:layout_height="0dp"
            android:background="#ff0000"
            android:gravity="center"
            app:layout_constraintBottom_toBottomOf="@id/ll_show"
            app:layout_constraintStart_toEndOf="@id/ll_show"
            app:layout_constraintTop_toTopOf="@id/ll_show">

            <TextView
                android:id="@+id/tv_item_delete"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="删除"
                android:textColor="#ffffff"
                android:textSize="16sp" />
        </LinearLayout>
    </androidx.constraintlayout.widget.ConstraintLayout>

    <data>

        <variable
            name="model"
            type="com.highlands.tianFuFinance.fun.home.HomeBean" />
    </data>
</layout>