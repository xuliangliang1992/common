<?xml version="1.0" encoding="utf-8"?>
<layout>

    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <LinearLayout
            android:id="@+id/ll_show"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            app:layout_constraintTop_toTopOf="parent">

            <TextView
                android:id="@+id/tv_good_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_margin="20dp"
                android:text="@{goodBean.goodName}" />

            <TextView
                android:id="@+id/tv_good_price"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_margin="20dp"
                android:text="@{String.valueOf(goodBean.price)}" />

            <TextView
                android:id="@+id/tv_good_num"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_margin="20dp"
                android:text="@{String.valueOf(goodBean.num)}" />
        </LinearLayout>
        <!-- 屏幕右侧外边部分，正常时在屏幕中处于不可见 -->
        <LinearLayout
            android:id="@+id/ll_hidden"
            android:layout_width="50dp"
            android:layout_height="0dp"
            android:background="#ff0000"
            android:gravity="center"
            app:layout_constraintBottom_toBottomOf="@id/ll_show"
            app:layout_constraintStart_toEndOf="@id/ll_show"
            app:layout_constraintTop_toTopOf="@id/ll_show">

            <TextView
                android:id="@+id/tv_item_delete"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="删除"
                android:textColor="#ffffff"
                android:textSize="16sp" />
        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

    <data>

        <variable
            name="goodBean"
            type="com.icloudwhale.cart.http.response.GoodBean" />

    </data>
</layout>
